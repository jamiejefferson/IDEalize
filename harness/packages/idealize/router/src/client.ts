/**
 * Client face of @idealize/router: the `SessionEventMap` and
 * `SessionProjectionMap` merges and the badge's types. Types only, so a
 * browser bundle names these values without pulling host code.
 * @module @idealize/router/client
 */
export type * from './projection-types.ts'
