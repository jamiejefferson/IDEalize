/**
 * Client face of @idealize/comm: the `SessionProjectionMap` merge for the
 * `agentName` projection and nothing else, so browser bundles can type the
 * projection without pulling host code.
 */
export type * from './projection-types.ts'
