/** The owl mascot's resting pose: frame 0 of the flipbook (owl-frames.ts). */
import { OWL_FRAMES } from './owl-frames.ts'

/** Data URI of the owl's resting frame. */
export const OWL_DATA_URI: string = OWL_FRAMES[0] ?? ''
